package CarRentalSystem.Vehicle;

public class Car extends Vehicle {
    public Car(String make, String model, int year, VehicleType vehicleType, double price) {
        super(make, model, year, vehicleType, price);
    }
}
